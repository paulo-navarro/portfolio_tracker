/** Base64 da chave pública X25519 do worker, injetada pelo vite.config.ts. */
declare const __SEALING_PUBLIC_KEY__: string
/** Identifica esta versão do app; troca invalida o cache guardado. */
declare const __BUILD_ID__: string
